apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vaultauth-team2
  namespace: team2
spec:
  vaultConnectionRef: vault
  method: kubernetes
  mount: kubernetes-prod-atl
  kubernetes:
    role: k3s-auth
    serviceAccount: vault-auth
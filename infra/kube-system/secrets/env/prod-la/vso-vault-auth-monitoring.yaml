apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vaultauth-monitoring
  namespace: monitoring
spec:
  vaultConnectionRef: vault
  method: kubernetes
  mount: kubernetes-prod-la
  kubernetes:
    role: k3s-auth
    serviceAccount: vault-auth